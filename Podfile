# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'BiometricLogin' do
  use_frameworks!

  pod 'SwiftKeychainWrapper', '~> 3.0'

  target 'BiometricLoginTests' do
    inherit! :search_paths
  end
end
